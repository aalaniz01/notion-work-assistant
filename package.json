{
  "name": "notion-work-assistant",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "packageManager": "pnpm@10.15.1",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "build:packages": "pnpm --filter @notion-work-assistant/priority-engine... --filter @notion-work-assistant/db... --filter @notion-work-assistant/notion... build",
    "check": "pnpm lint && pnpm format:check && pnpm typecheck && pnpm test && pnpm build && pnpm smoke:api",
    "check:all": "pnpm check && pnpm test:db",
    "db:generate": "pnpm --filter @notion-work-assistant/db db:generate",
    "db:migrate": "pnpm --filter @notion-work-assistant/db db:migrate",
    "dev": "pnpm build:packages && pnpm --parallel --filter @notion-work-assistant/api --filter @notion-work-assistant/web dev",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "notion:verify": "pnpm --filter @notion-work-assistant/notion build && node --env-file-if-exists=.env scripts/verify-notion-read-only.mjs",
    "smoke:api": "node scripts/smoke-api.mjs",
    "test": "pnpm build:packages && pnpm -r --if-present test",
    "test:db": "pnpm --filter @notion-work-assistant/db test:db",
    "typecheck": "pnpm -r --if-present typecheck"
  },
  "devDependencies": {
    "@eslint/js": "9.34.0",
    "eslint": "9.34.0",
    "eslint-plugin-react-hooks": "5.2.0",
    "globals": "16.3.0",
    "prettier": "3.6.2",
    "typescript": "5.9.2",
    "typescript-eslint": "8.41.0",
    "vitest": "3.2.4"
  }
}
